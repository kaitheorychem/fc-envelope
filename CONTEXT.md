# FC Envelope

無次元化振電相互作用・振動数・温度から、変位型調和振動子の Franck-Condon スペクトルを
計算する。同じ物理量を「連続なエンベロープ」と「離散な線の列」という 2 つの表現で扱う。

この文書は**語彙だけ**を定める。量どうしの関係式は `docs/theory/vcc.md`、ファイルと
API の形は `docs/dev/spec/interface.md`、そう決めた理由は `docs/adr/` にある。

## Language

### 系と振動

**振動モード (VibrationalMode)**:
基底状態の調和ポテンシャルにおける 1 つの基準振動。振動数 ε [cm⁻¹] と結合 S の組だけで
完全に特徴づけられる。
_Avoid_: 振動, フォノン, 規準振動

**系 (system)**:
計算対象の分子の振動モードの集まり。分子に固有で、温度・線形状・グリッドなどの条件を
含まない。同じ系を条件を変えて何度も計算する。
_Avoid_: 分子, モードセット, modes（集まりそのものを指すとき）

**基準振動**:
質量重み付きヘシアンを対角化して得られる、互いに独立な振動の座標系。本プログラムが
受け取る振動モードは常にこの基底で表されている。
_Avoid_: 基準座標, 正規モード

**変位型調和振動子**:
始状態と終状態のポテンシャルが、同一の曲率を保ったまま平行移動しているだけ、という
モデル。本プログラムはこのモデルから出ない（振動数変化も Duschinsky 回転も扱わない）。
_Avoid_: 変位調和振動子, シフト調和振動子

### 振電相互作用

**結合 (coupling)**:
2 つのポテンシャルの平行移動の程度を表す量。入力では 5 つの流儀を区別し、内部では常に
Huang-Rhys 因子へ正準化する。
_Avoid_: 変位, カップリング, 振電相互作用定数（流儀 V の固有名なので総称には使わない）

**流儀 (convention)**:
結合をどの量で書くかの選択。g・Δ・S・V・λ の 5 つがあり、無次元なもの（g, Δ, S）と
単位を持つもの（V, λ）に分かれる。単位を持つ流儀では coupling に単位を添える。
_Avoid_: 規約（E 軸の符号規約と紛れるため）, 慣習

**Huang-Rhys 因子 (S)**:
本プログラムの正準な結合量。g の符号が物理的に無意味なため、平方根の往復を避けて
これを内部表現に選んでいる。
_Avoid_: HR 因子, S 因子

**再配列エネルギー (λ)**:
λ = Σ_α S_α ε_α。片方のポテンシャルの底で測った他方のポテンシャルの高さ。
エンベロープの 1 次モーメントが −λ になる。
_Avoid_: 再編成エネルギー, reorganization energy と再配列を混在させること

### スペクトルの 2 表現

**エンベロープ (envelope)**:
連続関数 F(E)。時間相関関数 ρ(τ) のフーリエ変換として計算する。
_Avoid_: スペクトル（2 表現の総称なので片方を指すのに使わない）, 包絡線

**線 (line)**:
1 本の離散的な振電遷移。どのモードが何量子ぶん変化したかというラベルを持つ。
_Avoid_: ピーク, 遷移（物理的な遷移一般と紛れるため）, スティック

**FC 因子 (fc_factor)**:
振動状態間の重なり積分の 2 乗 Π_α |⟨m_α|U(g_α)|n_α⟩|²。熱占有を含まない。
_Avoid_: Franck-Condon 積分, 重なり積分

**密度 (density)**:
エンベロープの縦軸。単位は 1/cm⁻¹ で、∫F dE = 1 が成り立つ確率密度。
_Avoid_: 強度, intensity

**重み (weight)**:
線の縦軸。無次元で、全遷移にわたる総和が 1 になる確率質量。FC 因子に始状態の熱占有を
掛けたもの。
_Avoid_: 線強度, 強度, intensity

密度と重みは、同じ確率分布の連続版と離散版である。F(E) = Σ_ℓ w_ℓ · L(E − E_ℓ) という
関係にあり、両者を `intensity` と呼び分けないのはこのためである。

### エネルギー軸

**ZPL**:
zero-phonon line。どのモードも量子数を変えない遷移。本プログラムの E 軸はこれを原点に
取る。
_Avoid_: 0-0 遷移, origin

**サイドバンド**:
振動量子を正味で生成する線。E 軸の負側に立つ。
_Avoid_: フォノンサイドバンド, vibronic band

**ホットバンド**:
始状態に熱励起された振動量子がある線。E 軸の正側に立ちうる。
_Avoid_: 熱バンド

**単位 (unit)**:
入力に書かれた量がどの単位で表されているかの指定。エネルギーの単位のほか、流儀 V の
coupling には振電相互作用定数の単位がある。どの単位で書いても入力の境界で正準単位
（エネルギーは cm⁻¹、V は (cm⁻¹)^{3/2}）へ畳まれ、それより内側に単位は残らない。
単位の軸は項目ごとに独立で、入力ファイル全体で 1 つではない。既定の単位はそれを使う
ブロック（モード表・線形状・グリッド）が持ち、トップレベルには置かない。
_Avoid_: 次元（量の種類のことなので単位とは別）, スケール

**単位の種類 (unit kind)**:
その欄の値がどの次元の量かの区別。エネルギーと振電相互作用定数の 2 つがある。欄が
決め、coupling の欄では流儀が決める。別名がどの正式名を指すかはこれで決まる。
_Avoid_: 次元, 単位系

**正式名 (formal name)**:
名前だけで次元が読める単位の識別子。`cm^-1`、`hartree`、`hartree/(bohr*sqrt(m_e))` など。
プログラム内・実効設定・誤りの報告に現れるのはこれだけである。
_Avoid_: 正規名, 単位名（別名も含むので）

**別名 (alias)**:
入力ファイルでだけ使える慣習的な単位の書き方。`a.u.` はエネルギーの欄なら `hartree`、
V の欄なら `hartree/(bohr*sqrt(m_e))` を指す。入力の境界で正式名に置き換わる。
_Avoid_: 略称, エイリアス

**倍率 (scale)**:
単位の名前と組にして配列の要素として書く正の数。`[0.0001, "a.u."]`（値に添えるなら
`[-0.3, 0.0001, "a.u."]`）の `0.0001` のように、値全体をその数倍して読む（ADR-0078）。
_Avoid_: スケール, 係数（換算係数と紛れるため）

**正式形 (formal form)**:
別名を正式名に置き換え、倍率はそのまま残した単位の書き方。倍率が無ければ正式名の文字列、
あれば `[倍率, 正式名]`。検証を通った入力が持つ単位は常にこれで、読み直しても同じ正式形になる。
_Avoid_: 正準形（単位を消費し終えた後の表現を指すので）, 正規形

**有次元量 (quantity)**:
入力ファイルに書かれる、単位を伴う値。`[値, "単位"]` の組で書くと添えた単位で読み、
素の数値で書くと既定の単位（ブロックの `unit`、モード表なら列の既定の単位）で読む。どちらで
書いても入力の境界で同じ 1 つの形に畳まれる。
_Avoid_: 物理量（単位の軸を持たない温度も含む総称なので）, ペア, 組（書き方の一方を
指すので値そのものには使わない）

### 条件とつまみ

**線形状 (broadening)**:
1 本の線が E 軸上で持つ幅と形。時間領域では ρ(τ) に掛かる減衰因子として現れる。現在は
ガウス幅 σ だけを扱う。
_Avoid_: 広がり, 幅, lineshape と broadening の混在

**エネルギーグリッド (grid)**:
エンベロープを標本する E 軸上の点列。`e_min` / `e_max` が出力窓で、全域グリッドの取り方は
`grid.points` に書く。点数 `n`（2 の冪）を直接書くか、間隔 `de` から決めるかのどちらかで、
省略値も自動推定もない（ADR-0070）。
_Avoid_: 窓, レンジ, メッシュ

**全域グリッド (full grid)**:
FFT を掛ける 0 対称な E 軸上の点列。点数 N は 2 の冪で、広さ N·ΔE は出力窓以上になる。
端の折り返し（`edge_intensity_ratio`）を決めるのは出力窓ではなくこちらである。
_Avoid_: 全グリッド, 内部グリッド

**選択条件 (selection)**:
離散線のうちどれを保持するかを決めるつまみの組。エンベロープにおけるグリッドと同じ
位置を占める。
_Avoid_: 閾値, フィルタ, カットオフ

**モード表 (modes)**:
入力ファイル中の、振動モードを 1 行 1 モードで並べた表。coupling の流儀と、列ごとの
既定の単位を持つ。行は直接書くか、CSV から読む。CSV から読むときは列の並びと列ごとの
単位を書き、読んだ後は直接書いた行と区別がない（ADR-0079）。
_Avoid_: モードリスト, モードデータ（表の形を指さないので）

**入力ファイル (input file)**:
モードデータと計算条件を 1 つにまとめた、利用者が書くファイル。書式は TOML を基本とし、
JSON でも読める。どちらで書いても読んだ後は区別がない。書き始めるための雛形を
`fcenvelope template` が書き出す。
_Avoid_: 入力 JSON（書式が 1 つだった頃の呼び方）, 設定ファイル（実効設定と紛れるため）, インプット

**上書き (override)**:
入力ファイルの項目を、その実行のあいだだけ別の値に差し替えること。値はファイルと同じ
単位・流儀で読み、正準化の前に当てる。モードは対象にしない。
_Avoid_: オーバーライド, 指定, 設定（ファイルに書く行為と紛れるため）

**実効設定 (effective settings)**:
入力ファイルを読んで上書きを当て終えた時点の、その実行で実際に使われる設定。省略された
項目は既定値で埋まっている。計算の前に書き出され、そのまま入力として読み返せる。書式は
入力の書式によらず JSON（「無し」を書けるのが JSON だけであるため）。
_Avoid_: 設定ファイル（利用者が書く入力ファイルを指すため）, コンフィグ, 有効設定

### 図

**作図スクリプト (plot script)**:
計算の出力に添えて生成される、単独で動くプログラム。図の設定はすべてこの中にあり、
実行後も読み返せる。結果ファイルを実行時に読むので、条件を変えて計算し直しても同じ
設定のまま描き直せる。生成された後は利用者のもので、本プログラムは読み返さない。
_Avoid_: プロットスクリプト, 描画コード, テンプレート（生成前の雛形を指すので）

**雛形 (template)**:
パッケージが実物のファイルとして持つ原型。作図スクリプトの雛形（そのまま走る Python
ファイルで、生成のときに差し替わるのは生成ヘッダの区画だけ）と、入力ファイルの雛形
（そのまま計算が通る TOML ファイルで、項目ごとに 1 行のコメントが付く）の 2 つがある。
どちらも書き出した後は利用者のもので、本プログラムは読み返さない。
_Avoid_: ひな型, スケルトン, テンプレート

**生成ヘッダ (generated header)**:
雛形の中でマーカーに挟まれた区画。データと出力先の位置だけが書かれる。生成が触るのは
ここだけで、それより下は写すだけである。
_Avoid_: ヘッダ（結果ファイルの来歴と紛れるため単独では使わない）, 前文

### 品質

**正準形 (canonical)**:
流儀と単位を消費し終えた後の表現。振動モードなら (ε [cm⁻¹], S)。結果ファイルの入力
エコーは常にこの形で書かれる。
_Avoid_: 標準形, 正規形

**版 (version)**:
パッケージそのものの番号 `X.Y.Z`。`pyproject.toml` に書かれたものが唯一の情報源で、
リリースのときに同じ番号の git タグ `vX.Y.Z` がそれを指す（ADR-0073）。入力・結果
ファイルの `schema_version` とは別の数である。
_Avoid_: バージョン, リビジョン, スキーマ版（形式の側の数なので混ぜない）

**来歴 (provenance)**:
計算がいつ、どの版で行われたかという情報。物理量でも計算条件でもなく、計算を行った時点で
確定する。
_Avoid_: メタデータ, ヘッダ

**診断値 (diagnostics)**:
計算そのものは完走させたうえで、結果の信頼可否を判定するために記録する数値。結果
ファイルだけを見て判断できることを目的とする。
_Avoid_: メトリクス, 統計量, 品質指標

**完全性 (completeness)**:
理論上 1 になるはずの和が、打ち切りの結果いくつまで拾えているか。振動梯子の長さと、
保持した線の網羅性の 2 箇所で使う。
_Avoid_: カバレッジ, 網羅率

**節目 (stage)**:
ログに開始と完了を残す処理の区切り。ファイルの読み書き、重い数値計算、図の書き出しのように、
1 回の実行に数回しか現れないものだけを指す。開始と完了が対になり、完了行がないことが
「そこで止まった」を意味する。ループの内側は節目にしない。
_Avoid_: フェーズ, 段階（ADR の作業計画の語と紛れるため）, 進捗

**痕跡 (trace)**:
不具合が起きたときに後から読むための、節目の記録。診断値が「結果を信じてよいか」を
答えるのに対し、痕跡は「どこまで進んだか」を答える。
_Avoid_: ログ（記録一般を指すのでファイルの中身には使わない）, デバッグ出力
