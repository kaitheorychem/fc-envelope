# 公開 API は自由関数とし、結果クラスを純粋なデータ容器に保つ

- 日付: 2026-07-25
- 状態: 受理（責務の分担は有効。関数名は ADR-0031 が改める）

「計算・保存・読み込み・描画」を自由関数として公開し、結果クラスに I/O と描画の責務を持たせない。これにより matplotlib への依存を `plotting.py` に隔離できる。

描画関数は `Figure` を返すのみでファイル保存はしない。`ax` を受け取れるため複数条件を 1 枚に重ね描きでき、ノートブックでもそのまま表示される。保存は呼び出し側（CLI）の責務とする。
