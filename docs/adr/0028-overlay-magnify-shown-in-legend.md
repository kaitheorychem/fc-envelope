# 棒の拡大倍率は必ず凡例に出す

- 日付: 2026-09-16
- 状態: 受理

モードが多い系や高温では線が σ の中に埋もれ、棒が軸の底に潰れて構造が読めなくなる。このため棒だけを拡大する `magnify` を持つ。ただし**倍率は凡例に `(×N)` として必ず出す**。分光のスペクトル図で拡大部に倍率を添えるのと同じ作法で、ADR-0027 が担保した「高さが読める」性質を、拡大したことを隠さない形で保つ。既定は 1.0（無変換）で、そのときは凡例に何も足さない。
