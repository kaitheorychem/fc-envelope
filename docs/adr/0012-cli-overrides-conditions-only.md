# CLI が上書きできるのは計算条件だけとし、モードは上書きしない

- 日付: 2026-07-25
- 状態: 受理（上書き対象は ADR-0035 が読み替える）

CLI で上書きできるのは `conditions` のフィールドのみ。`modes` は上書き対象にしない。モードは分子固有のデータであり、コマンドラインで差し替えると「どの分子を計算したか」が履歴に残らなくなるため。

`plot` サブコマンドを分けることで、重い計算をやり直さずに見た目だけ調整できる。終了コードは `0` 正常 / `1` `FCEnvelopeError` / `2` 使用法エラー。
